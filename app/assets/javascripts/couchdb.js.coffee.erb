jQuery(document).ready () ->
  _.extend $.couch, urlPrefix: '/db'
  _.extend Backbone.couch_connector.config,
    db_name : <%= CouchRest::Model::Base.database.name.to_json %>
    ddoc_name : 'Backbone'
    view_name : 'by_collection'
    global_changes : true
    base_url : '/db'
